# 开发日志 — 2026-06-04

## 完成事项
- [x] SQL 基础知识整理（存入 `my_test/sql-basics.md`）
- [x] 数据库结构检查，用户通过 sqlite3 命令行了解表结构
- [x] **子文件夹（嵌套文件夹）功能开发**
  - [x] 数据库迁移：folders 表添加 `parent_id` + `position` 列
  - [x] `add_folder()` 支持 `parent_id` 参数，自动计算同级 position
  - [x] `get_all_folders()` 按 `parent_id, position, name` 排序
  - [x] `delete_folder()` 级联删除子文件夹（BFS 查找所有后代）
  - [x] 新增 `get_subfolder_ids(folder_id)` — 返回所有后代 ID 集合
  - [x] 新增 `update_folder_position()` + `move_folder()` — 修复旧代码遗留 bug
  - [x] 新建 `src/ui/folder_tree.py` — 树形侧边栏组件（QTreeWidget）
  - [x] `main_window.py` 布局重构：侧边栏(200px) + 右侧内容区
  - [x] `history_panel.py` 右键"移至文件夹"菜单支持嵌套
  - [x] `floating_window.py` 收藏菜单递归显示文件夹树
  - [x] FOLDER_MIME 导入路径从 folder_bar 迁移到 folder_tree

## 技术决策
- 树形侧边栏替换水平标签栏，支持无限层级嵌套
- 选中父文件夹只显示直属提示词（不递归）
- 保持 `folder_changed(int | None)` 信号签名完全兼容
- folder_bar.py 保留但不再被任何代码导入

## 涉及文件
| 文件 | 操作 |
|------|------|
| `src/db/database.py` | 修改：迁移 + add_folder/get_all_folders/delete_folder 更新 + 3 个新函数 |
| `src/ui/folder_tree.py` | 新建：FolderTree 树形侧边栏组件 |
| `src/ui/main_window.py` | 修改：布局重构，FolderBar → FolderTree |
| `src/ui/history_panel.py` | 修改：+ _build_folder_menu 递归菜单，导入路径更新 |
| `src/ui/floating_window.py` | 修改：+ _build_folder_favorites 递归菜单 |

## 待办
- [ ] 用户手动测试子文件夹创建、删除、拖拽等交互
- [ ] 推送代码到远程仓库
